目前使用的基础镜像都是基于ubuntu24.04的

1. 安装基础环境

```bash
apt update 
apt upgrade 
apt install curl wget build-essential pkg-config libssl-dev
```

2. 安装rust

```bash
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
```

3. 初始化项目

```bash
cargo new back_api
```

